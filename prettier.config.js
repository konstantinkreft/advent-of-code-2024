/** @type {import("prettier").Config} */
const config = {
  semi: true,
  arrowParens: 'avoid',
  trailingComma: 'all',
  singleQuote: true,
  endOfLine: 'lf',
  bracketSpacing: true,
  printWidth: 80,
  useTabs: false,
  quoteProps: 'as-needed',
  tabWidth: 2
}

export default config
